# Compatibility-only pointer. Canonical dependencies are declared in the root
# pyproject.toml. The supported default offline development-prototype path uses
# only the Python standard library and intentionally installs no provider,
# browser-automation, network-client, or TUI dependency.
