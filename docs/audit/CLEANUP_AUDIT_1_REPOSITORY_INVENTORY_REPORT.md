# CLEANUP-AUDIT-1 Repository Inventory Report

## Status

Audit-only. No deletion performed.

## Branch / HEAD / Repo State

- Branch: `feature/m2-b0-provider-critic-inert-core`
- HEAD: `2d4d7f9 feat: bind artifact write to durable audit append`
- Repo state at baseline: clean and synced with `origin/feature/m2-b0-provider-critic-inert-core`
- Tracked files: `1164`
- Files under `find . -maxdepth 4 -type f`: `3690`
- Non-ignored untracked files: none found

## Summary

The repository has no dirty tracked files and no non-ignored untracked files.

The main cleanup candidates are ignored local artifacts:

- `runtime/.venv/` local Python virtual environment, about `66M`, not tracked
- `__pycache__/` directories, `196` directories found
- `*.pyc` files outside `runtime/.venv/`, `250` files found

No `*.log`, `*.patch`, `*.diff`, temp, backup, reject, or editor backup files were found outside the ignored local environment scan. No empty files were found outside `runtime/.venv/`.

Tracked audit reports, closure reports, forensic exports, corpus JSONL files, and knowledge JSONL files appear intentional and should not be removed without a separate documentation/archive review.

## Candidate Files/Directories

### A - Safe to delete immediately

| Path | Reason | Tracked? | Risk |
|---|---|---:|---|
| `runtime/.venv/` | Local Python virtual environment; ignored by `.gitignore`; not tracked; about `66M`. | No | Low if the developer can recreate/install dependencies locally. |
| `**/__pycache__/` | Generated Python bytecode cache; ignored by `.gitignore`; `196` directories found. | No | Low; regenerated by Python/test runs. |

### B - Probably safe, needs human confirmation

| Path | Reason | Tracked? | Risk |
|---|---|---:|---|
| None found in this audit. | No non-ignored untracked temp, patch, diff, log, backup, or scratch files were found. | N/A | N/A |

### C - Keep, Maybe Archive Later

| Path | Reason |
|---|---|
| `docs/audit/` historical reports | Large audit history, including RED-1, Macrostep 1, 2A, and 2B evidence. Old does not mean stale; keep as audit trail. |
| `archive/forensic_exports/` | Tracked forensic export bundle with commit history; suitable for later archive review, not deletion in cleanup. |
| `archive/historical_planning/` | Historical planning material; referenced by archive documentation. |
| `docs/nms/` and `docs/stress_tests/` | Some overlap in names such as `FAILURE_MODES.md`, `MODEL_AUDIT_MATRIX.md`, and roadmap material, but `docs/FRAMEWORK_SURFACE_MAP.md` marks these areas as move-later/reviewer documentation. |
| `docs/reports/FINAL_URL_HANDOFF_PATCH.md` | Name looks patch-like, but it is a tracked Markdown handoff document referenced in manifests/history. |
| `docs/historical/adr_legacy/` | Duplicate ADR names exist by design as historical legacy ADRs; keep unless documentation architecture is reviewed. |

### D - Do Not Delete

| Path | Reason |
|---|---|
| `runtime/safety/`, `runtime/schemas/` | Current controlled-agent and safety runtime. |
| `tests/` excluding ignored cache directories | Active validation suite; full suite currently passes. |
| `docs/audit/MACROSTEP_2A_*`, `docs/audit/MACROSTEP_2B_*` | Current hardening and durable-audit evidence chain. |
| `docs/audit/M6_A_*` through `M10_A_*` | Macrostep 1 controlled-agent audit history. |
| `tests/corpus/*.jsonl` | Tracked test corpora, intentionally discovered by JSONL scan. |
| `runtime/knowledge/memory_hats/*.jsonl` and `knowledge/**/*.jsonl` | Tracked knowledge/corpus data, not log trash. |
| `.github/`, `web/`, `.gitignore`, README/governance/security docs | Project infrastructure and reviewer-facing documentation. |

### E - Needs Deeper Review

| Path | Question |
|---|---|
| `docs/nms/` vs `docs/stress_tests/` | Some duplicate topic names exist. Are both layers still needed as separate reviewer/grant vs stress-test documentation packages? |
| `archive/forensic_exports/reports_forensic_export/source_export/` | This appears to duplicate many tracked source files as a preserved forensic export. Keep for now, but confirm long-term archive policy. |
| Tracked PDFs under `docs/audit/`, `docs/reviewer/`, `docs/reports/`, and `runtime/knowledge/source/` | These may be legitimate evidence/source artifacts, but binary files should be reviewed for size, provenance, and whether generated PDFs should remain tracked. |
| Duplicate basename families such as `README.md`, `ROADMAP_4_MONTHS.md`, `FAILURE_MODES.md`, and legacy ADR names | Likely intentional because they live in different documentation domains, but worth a later doc-authority review. |

## .gitignore Recommendations

Current `.gitignore` already covers:

- `__pycache__/`
- `*.pyc`
- `.pytest_cache/`
- `.venv/`
- `.env`, `.env.*`, secrets, keys, local runtime state, screenshots, logs under `/runtime/logs/`

Recommended additions to consider in a later explicit `.gitignore` cleanup commit:

- `.mypy_cache/`
- `.ruff_cache/`
- `.DS_Store`
- `Thumbs.db`
- `*.tmp`
- `*.temp`
- `*.bak`
- `*.backup`
- `*.orig`
- `*.rej`
- `*~`
- `*.log` if project policy says logs should never be tracked anywhere

No `.gitignore` changes were made in this audit.

## Cleanup Plan Proposal

Safe next cleanup step, not executed here:

1. Confirm whether local `runtime/.venv/` can be removed and recreated when needed.
2. Remove ignored local artifacts only: `runtime/.venv/` and `**/__pycache__/`.
3. Re-run `git status --ignored -sb`, `python3 -m compileall -q runtime tests`, `python3 -m unittest discover -s tests`, and `node --check web/app.js`.
4. Keep tracked docs/audit/archive material untouched until a separate documentation authority/archive review.

## Safety Statement

No runtime behavior changed. No tests changed. No files were deleted. No files were moved. No shell/provider/browser/git/cloud capability was added. This report is inventory-only.
